apiVersion: v1
kind: Service
metadata:
  labels:
    app: quorum-explorer
    environment: Development
  name: quorum-explorer
  namespace: jobs
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: quorum-explorer
  sessionAffinity: None
  type: ClusterIP
